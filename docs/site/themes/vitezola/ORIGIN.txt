Vendored from github.com/newlix/vitezola @ c493405d79f1a89bf976cdc7c25e969be76d34aa
("Translate README to English"). Stripped: content/ demo site, scripts/, .git.

LOCAL PATCHES — re-apply after a re-vendor (upstream PRs welcome):
1. templates/base.html: an `{% if config.extra.extra_css %}` block after the
   two stylesheet links — the site loads shadless-site.css through it
   (docs/site/config.toml [extra] extra_css).
2. templates/partials/doc_body.html: `{% if doc.title and not doc.content is
   starting_with(pat="<h1") %}` — the generated markdown opens with its own
   H1; without this every page prints the title twice.

A re-vendor that drops patch 1 collapses every demo iframe to the browser's
150px default; dropping patch 2 duplicates every page title.
