// react-textarea-autosize stub: DOM-shape pass-through (autosize is
// runtime behavior — the static oracle render only needs a textarea).
import * as React from "react"

export default function TextareaAutosize(props) {
  return React.createElement("textarea", props)
}
