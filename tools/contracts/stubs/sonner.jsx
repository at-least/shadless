// sonner stub: toast demos are interactive (toast fires on click) — the
// static oracle DOM needs only the trigger elements. toast = noop,
// Toaster renders nothing.
import * as React from "react"

export function toast() {}
export function Toaster() { return null }
