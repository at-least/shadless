{
  "//": "Type-checks the src/*.mjs tool sources in place (checkJs). No emit — these files run as plain ESM under node; typechecking is a lint, not a build step. dist/ output is untouched by definition.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": true,
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "allowImportingTsExtensions": true,
    "target": "es2023",
    "lib": ["es2023"],
    "types": ["node"],
    "skipLibCheck": true
  },
  "include": ["src/**/*.mjs", "src/**/*.js.d.ts"]
}
